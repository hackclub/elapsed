<Page
	x:Class="Riverside.Elapsed.App.Views.RecordingPage"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:utu="using:Uno.Toolkit.UI"
	Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
	Loaded="OnPageLoaded">
	<Grid utu:SafeArea.Insets="VisibleBounds" Margin="24">
		<Border
			MaxWidth="560"
			Padding="28"
			HorizontalAlignment="Center"
			VerticalAlignment="Center"
			Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
			BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
			BorderThickness="1"
			CornerRadius="10">
			<StackPanel Spacing="14">
				<TextBlock FontSize="28" FontWeight="SemiBold" Text="Recording" />
				<TextBlock
					FontSize="13"
					Opacity="0.7"
					Text="{Binding SourceName, FallbackValue='No source selected'}" />

				<Border
					Padding="16"
					Background="{ThemeResource ControlFillColorSecondaryBrush}"
					CornerRadius="8">
					<TextBlock
						FontFamily="Consolas"
						FontSize="36"
						FontWeight="SemiBold"
						HorizontalAlignment="Center"
						Text="{Binding ElapsedDisplay}" />
				</Border>

				<TextBlock Opacity="0.75" Text="{Binding Status}" TextWrapping="Wrap" />

				<StackPanel Orientation="Horizontal" Spacing="10">
					<Button
						Command="{Binding PauseResumeCommand}"
						Content="{Binding PauseResumeText}"
						IsEnabled="{Binding IsRecording}" />
					<Button
						Command="{Binding StopCommand}"
						Content="Stop"
						IsEnabled="{Binding IsRecording}"
						Style="{ThemeResource AccentButtonStyle}" />
					<Button Command="{Binding BackCommand}" Content="Back" />
				</StackPanel>
			</StackPanel>
		</Border>
	</Grid>
</Page>
